const { GoogleGenerativeAI } = require('@google/generative-ai'); const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY); const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' }); model.generateContent('Provide precise book catalog details for the book with Title "madol duwa". Respond ONLY in exact JSON format: {"title": "Book Name", "author": "Author"}').then(r=>console.log(r.response.text())).catch(console.error);
